const student = {
	name : 'Duc hung',
	rank : '100',
	color : 'Black',
};

const keys = Object.keys(student);
const entries = Object.entries(student);
const value = Object.values(student);

console.log(keys);
console.log(entries);
console.log(value);
